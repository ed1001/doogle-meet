name: remote ssh command
on: [push]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: executing remote ssh commands using password
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.APPUSER_SSH_PRIVATE_KEY }}
          username: appuser
          password: Eduardin321
          script: "echo $PATH"
